<%= simple_form_for(@user_creature) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :creature %>
    <%= f.input :nickname %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
