<h1>Listing battles</h1>

<table>
  <thead>
    <tr>
      <th>Creature location</th>
      <th>User creature</th>
      <th>User creature current health point</th>
      <th>Creature current health point</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @battles.each do |battle| %>
      <tr>
        <td><%= battle.creature_location_id %></td>
        <td><%= battle.user_creature_id %></td>
        <td><%= battle.user_creature_current_health_point %></td>
        <td><%= battle.creature_current_health_point %></td>
        <td><%= link_to 'Show', battle %></td>
        <td><%= link_to 'Edit', edit_battle_path(battle) %></td>
        <td><%= link_to 'Destroy', battle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Battle', new_battle_path %>
